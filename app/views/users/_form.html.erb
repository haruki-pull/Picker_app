<%= form_for(user) do |f| %>
<%= render 'shared/error_messages', object: f.object %>

　　<%= f.label :名前 %>
    <%= f.text_field:name %>

　　<%= f.label :Eメール %>
    <%= f.email_field:email %>

    <%= f.label :あなたのパスワード %>
    <%= f.password_field :password %>

    <%= f.submit %>

<% end %>