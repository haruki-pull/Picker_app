<%= form_for(@souvenir) do |f| %>

　　<%= f.label :name %>
　　<%= f.file_field :name %>

　　<%= f.label :description %>
　　<%= f.file_field :description %>

　　<%= f.label :picture %>
　　<%= f.file_field :picture %> 

<% end %>